# Pipewright Redesign — Workplan & Handoff (START HERE)

**Last updated:** 2026-06-18
**Purpose:** Orientation and resume-point for the redesign effort. If you have no other context, read this first — it explains *what* we're doing, *how* we decided to execute it, *where we stopped*, and *what's next*. It is an index over the other planning docs, not a replacement for them.

---

## TL;DR

- **Latest update (2026-06-18): Row 16b soak evidence run is COMPLETE
  (docs-only).** The ledger-metrics queries were run `SELECT`-only against a
  throwaway read-only copy of `backend/db/pipewright.db` (no SQL errors; no
  code/flag/schema/runtime change). **Outcome: keep all dormant flags OFF.** The
  dev DB (~255 runs over ~3 weeks) reads as **seeded/synthetic** — sub-minute
  approval latency, frequent gate timeouts, small `run_turns`/`chunk_attempts`
  samples. `INFRA_ERROR` retry/recovery has **zero evidence**; prompt-cache
  activation has **no evidence** (`llm_call_provenance` is incomplete and
  effectively DeepSeek/coder-only, with no Anthropic/Gemini provenance); Row 12
  omission shows **no meaningful coder/planner pressure**; the Row 16 auto
  hygiene path has no evidence and the manual `run_outcome` rejection rate is a
  quality yellow flag; the reviewer acknowledgement path exists but has low
  recorded ack volume. Results recorded in
  `docs/metrics/ledger-metrics-queries.md` §12. Real-traffic soak data (and
  broader provenance coverage) is needed before revisiting any activation.
- **Latest update (2026-06-18): Row 16b ledger metrics / observability queries
  doc is COMPLETE (docs-only).** Added
  `docs/metrics/ledger-metrics-queries.md` with SELECT-only SQLite queries over
  existing ledger metadata for maintainers to collect evidence before deciding
  whether to activate dormant features. It covers stage-profile/scoped-
  verification health, attempt outcomes, `INFRA_ERROR` recovery, approvals,
  steer/retry/refinement usage, reviewer acknowledgement proxies, Row 16
  suggestion yield, Row 12 omission pressure, prompt-cache opportunity, plan-turn
  activity, and stuck runs. It activates no dormant flags, changes no runtime
  behavior, schema, policy, memory retrieval, prompt builder, approval/final
  approval/Git/PR behavior, event persistence, FTS/Row 19, or Row 23.
- **Latest update (2026-06-17): Phase 2G Run Detail Product UI is COMPLETE &
  MERGED, frontend presentation/composition only.** The design spec is complete
  in `docs/design/phase-2g-run-detail-product-ui.md`. **PR-1** merged the
  two-column cockpit/context shell; **PR-2** merged Running and Needs-review
  context rail trust facts; **PR-4** merged Done-state PR de-duplication and the
  authoritative PR rail; **PR-5** merged the Failed-state failure rail; **PR-3**
  merged decision evidence near the approval cockpit; and **PR-6** merged
  visual/register polish plus Plain/Developer mode cleanup. Final state: Run
  Detail is organized around the cockpit, safety overview, context rail,
  decision evidence, timeline, and collapsed audit/details. **Invariants held:**
  no backend behavior changes, no mutation handler changes, no approval/final
  approval/Git/PR behavior changes, no new actions, no event persistence / Phase
  2F PR-5 work, no memory retrieval changes, no FTS/Row 19 changes, and no Row
  23 work. **Validation recorded:** build/lint/diff checks passed per slice;
  PR-3 demo-smoke passed all 10 checks; PR-6 SSR smoke passed
  running/final-approval/done/failed across Plain and Developer modes; protected
  path checks confirmed frontend/docs-only scope per slice.
- **Latest update (2026-06-17): Phase 2F Thread UI / Run Timeline is COMPLETE &
  MERGED, read-only, through PR-0..PR-4 plus review fixes PR-A/PR-B/PR-C.** PR-0 added
  the backend read-only `GET /runs/{run_id}/timeline` derived from existing persisted
  tables; PR-1 added the frontend `useRunTimeline` hook + additive `RunTimeline`; PR-2
  added the read-only `RunTimelineDetail` master-detail panel; PR-3 promoted the
  timeline to the primary Run Detail layout and made the existing
  `OperatorAttentionPanel` sticky/prominent; PR-4 added the Plain English / Developer
  view toggle. PR-A fixed backend timeline correctness/redaction tests; PR-B fixed
  persisted/live dedupe and timeline refresh; PR-C fixed redaction polish, sticky
  height, the `localStorage` guard, and a11y. **Invariants held:** no PR-5 / event
  persistence, no schema or event table, no backend writes, no POST lifecycle handler
  changes, no approval/final-approval/Git/PR behavior change, no memory-retrieval
  change, no FTS/Row 19 activation, and no Row 23/vector work — the only backend
  surface added is the single read-only GET. Design brief + closeout:
  `docs/design/phase-2f-thread-ui.md` (§13). **PR-5 (fine-grained event persistence)
  remains deferred** and is the only unshipped slice; Row 16 PR-C activation and
  Row 23 vector memory also remain deferred.
- **Latest update (2026-06-16): §23 row 19 MemoryRetriever/FTS is COMPLETE &
  MERGED, default-off and explicit-rebuild-only.** PR-A landed the guarded inert
  SQLite FTS scaffold plus explicit rebuild lifecycle; PR-B landed the
  `MemoryRetriever` seam with deterministic rung-0; PR-C landed FTS rung-1 behind
  `PIPEWRIGHT_MEMORY_FTS_RETRIEVAL_ENABLED`. Flag-off is byte-identical to
  rung-0/current memory behavior. Flag-on uses FTS only as an advisory ordering
  signal over the canonical rung-0 candidate set: it never adds, drops, caps, or
  cross-projects candidates, and mandatory/safety facts are never scored, demoted,
  omitted, or dropped. Explicit rebuild/population is required for flag-on to have
  effect. There is no rebuild-on-write, lazy rebuild-on-read, endpoint, frontend,
  `schema.sql` FTS DDL, approval/execution/final-approval/Git/PR behavior change,
  or Row 23 vector memory. Row 16 PR-C activation, Row 23, and thread UI remain
  deferred.
- **Latest update (2026-06-16): Row 19 FTS populate/soak follow-up is COMPLETE &
  MERGED.** PR-1 added the explicit guarded manual FTS rebuild CLI. PR-2 added
  the read-only compare/seed soak harness and
  `docs/design/row-19-fts-soak-results.md`. Seeded soak passed with identical
  included set, identical mandatory tier, only relevance-tier ordering changed, no
  cross-project facts, and deterministic output. Real-project soak safely fell
  back with zero ordering delta. FTS retrieval remains default-off/dormant. There
  is no activation trigger, rebuild-on-write, lazy rebuild-on-read, default-on
  flip, endpoint/frontend/`schema.sql`/boot-migration populate, or Row 23
  vector/embedding work. The later approval-write-path rebuild trigger remains
  deferred and should not be started unless future soak shows real value.
- **Latest update (2026-06-16): §23 row 7b plan-gate turns plus plan-version lineage are COMPLETE & MERGED, end-to-end but default-off.** PR-A/PR-B/PR-C/PR-D plus stabilization Slices A/B/C landed the plan-turn engine, backend route, frontend "Revise plan" affordance, env wiring, lineage endpoint, approval-version binding, and frontend lineage display. Backend/API/DB smoke with `PIPEWRIGHT_PLAN_TURNS_ENABLED=true` passed; later manual smoke confirmed flag-off disabled behavior and flag-on revise/history/approval behavior. Safety invariants held: no auto-execution, no approval blocker from lineage/stamping, and no final-approval bypass. **Default remains off.** Row 16 PR-C activation, Row 23, and thread UI remain deferred; Row 19 is complete per the row-19 closeout above.
- **Latest update (2026-06-14): ROW 12 COMPLETE — PR-A, PR-B, and PR-C are all merged; D5 confirmed; PR-C ships dormant-by-default.** The maintainer affirmatively confirmed D5 (single-sourced in proposal §24's "D5 confirmed" note): omission semantics + small-store grace (12, counted over non-mandatory in-policy relevance facts) + priority-based pinning (`priority ≤ 10`) + a single global off-switch. PR-C adds, behind one default-off policy flag `MEMORY_RELEVANCE_OMISSION_ENABLED = False` that gates BOTH behaviors: (1) **relevance omission** — a zero-signal relevance fact (zero path-overlap **and** zero token-overlap) is excluded with `not_relevant_to_request`, but only when the flag is on, the request carries signal, the relevance-candidate count exceeds `MEMORY_SMALL_STORE_GRACE_THRESHOLD` (12), **and** at least one relevance fact carries signal (all-zero signal omits nothing); and (2) **priority-based human-pinning** — a fact with `priority ≤ MEMORY_PIN_PRIORITY_THRESHOLD` (10) joins the mandatory tier (never scored / omitted / budget-dropped), so the mandatory-overflow error generalizes to "mandatory tier (safety + pinned)". **The flag ships `False`, so default behavior is byte-identical to PR-B** (all PR-A/PR-B goldens hold). No schema, frontend, per-project setting, planner/triage/prompt-preview plumbing, adaptive-budget activation, `MemoryRetriever`/FTS/vector, or memory mutation; the scoring `files_expected` still never reaches `scope_guard`. Omission is coder-only in practice. Changed files: `backend/pipeline/policy.py` (3 constants), `backend/memory/prompt_builder.py`, `backend/tests/test_memory_selection_scaffolding.py` (`coder.py` unchanged — omission rides the `request_context` it already passes). **Rollback:** keep/return `MEMORY_RELEVANCE_OMISSION_ENABLED = False`. **Next step: a maintainer / Claude roadmap review before opening any new row or activating the flag** — the only outstanding Row 12 action is operational (flipping the flag is a soak decision); do not auto-start Row 16 PR-C, Row 23, or the §21 thread UI. Row 19 has since completed; see the 2026-06-16 row-19 closeout above.
- **Superseded 2026-06-14 (by the Row 12 PR-C closeout above): Row 12 PR-B is COMPLETE.** Row 12 PR-B added request-aware memory **relevance ordering** (ordering only — no omission), building on the PR-A scaffolding. `run_coder` builds a `RequestContext` from `plan.goal` (title), `plan.feature_description`, `files_to_modify + files_to_create` (`files_expected`), and `continuation_context` (steer), and passes it to `build_project_memory_block_detailed`; the planner, triage, and prompt-preview call sites are **untouched**. A populated `request_context` reorders **only** the non-mandatory relevance tier by a deterministic rung-0 signal — path-token overlap (primary), then content-token Jaccard (secondary), reusing the `memory_trust` `_content_tokens` / `_jaccard` helpers — tie-broken by the existing `(category, scope, priority, created_at)` key. `request_context=None` stays **byte-identical**; **all-zero overlap preserves the legacy order**; the `security` / `forbidden_paths` mandatory tier stays first and is **never scored, reordered, or budget-dropped**. **No relevance omission, no `not_relevant_to_request` emission** (exclusions keep existing reasons such as `budget_dropped` / `category_not_allowed_for_role`), **no D5 activation, no human-pinning, no per-project off-switch, no grace threshold / relevance floor, no adaptive-budget activation, no `MemoryRetriever` / FTS / vector / embedding work, no planner/triage/prompt-preview plumbing, and no schema/frontend/gate/scope/Git/PR/memory-mutation change**; the scoring `files_expected` never reaches `scope_guard`. Changed files: `backend/memory/prompt_builder.py`, `backend/pipeline/coder.py`, and the two test files (`test_memory_selection_scaffolding.py`, `test_coder.py`). **Deferred:** Row 12 **PR-C** (relevance omission / human-pinning / off-switch — **requires D5 confirmation before implementation**), plus rows **16** (post-run hygiene), **19** (retriever + FTS), **23** (vector/embedding), and the §21 thread UI (**22b–22e**). **Next step:** pause for maintainer **D5** decision/review before opening PR-C.
- **Superseded 2026-06-14 (by the Row 12 PR-B closeout above): Row 12 PR-A is COMPLETE.** Row 12 PR-A shipped no-op-by-default request-aware memory-selection scaffolding only: `request_context` exists but remains dormant; `request_context=None` preserves existing memory injection behavior; memory budgets and the token estimator are single-sourced in `policy.py`; adaptive budget scaffolding exists but remains disabled; `security` / `forbidden_paths` are mandatory safety facts and cannot be `budget_dropped`; mandatory safety overflow uses the typed `MandatoryMemoryBudgetExceeded`; prompt-preview catches mandatory overflow and returns HTTP 422; and `not_relevant_to_request` is defined but dormant and not emitted. PR-A included **no relevance ordering, no relevance omission, no D5 activation, no human-pinning, no per-project off-switch, no orchestrator `request_context` plumbing, and no schema/frontend/gate/scope/Git/PR/memory mutation/vector/FTS/thread UI changes**. **Deferred (at PR-A closeout; PR-B has since landed — see the top bullet):** Row 12 **PR-C** (relevance omission / human-pinning / off-switch — **requires D5 confirmation before implementation**), plus rows **16** (post-run hygiene), **19** (retriever + FTS), **23** (vector/embedding), and the §21 thread UI (**22b–22e**). **Next step (superseded):** the next step is the **D5** decision/review before Row 12 PR-C.
- **Latest update (2026-06-14): Row 11 is COMPLETE.** §23 **order-row 11 — detection rules-as-data** has landed as three behavior-preserving sub-slices plus fixture hardening: **PR-A** extracted `_collect_candidates` detection into `backend/memory/detection_rules.py` with ordered six-field parity; **pre-PR-B fixture hardening** covered the missing detector branches; **PR-B** added pure/read-only advisory repo reality signals for non-DB dimensions, compute-on-read through existing memory injection analysis `reality_warnings`, with `db_engine` / DB conflict gate behavior unchanged and no memory mutation; **PR-C** refactored `backend/pipeline/test_command_detection.py` into ordered detector rules with byte-identical `suggested_test_command` behavior. PR-C included **no PR-C2/new detector coverage** and no classifier/runtime-validation/frontend/schema/memory/gate/scope/Git/PR behavior changes. **Superseded 2026-06-14:** Row 12 PR-A subsequently opened and is now complete (see the top TL;DR bullet). Still deferred: Row 12 **PR-C** (the PR-B relevance-ordering slice has since landed), **16** (post-run hygiene), **19** (retriever + FTS), **23** (vector/embedding), and the §21 thread UI (**22b–22e**).
- **Prior update (2026-06-14, superseded by the Row 11 closeout above): Area B (Pass 2 — Memory) had begun; the M5 gate shipped and Row 11 PR-A had merged.** §23 **order-row 7 — the M5 suggestion-quality gate** (Pass 2 §11.3) is **IMPLEMENTED + MERGED via PR #292** (commit `65911b3` "Add memory suggestion quality gate"; design `23b6c02`): a deterministic, string-only scorer (`backend/memory/suggestion_quality.py`), an objective-junk floor + per-run caps (`HANDOFF_SUGGESTION_CAP = 5` / `RUN_SUGGESTION_TOTAL_CAP = 8`) + score-ranking inside `_handoff_candidates`, the **structured coder handoff channel** (`SuggestedMemoryEntry {content,category,scope,rationale}` with legacy bare-string tolerance), and an additive nullable `quality_score` column on `memory_suggestions` — all **pending-only**, with the #21B content gate never bypassed (`blocked_count` vs `floored_count` kept distinct). **Post-M5 self-use smoke / closeout recorded** in `docs/testing/memory-m5-suggestion-quality-smoke.md`, satisfying the Appendix E.1 hard-stop. **Two non-blocking loose ends tracked there:** (1) the planner still solicits bare-string suggestions, but they do **not** feed the handoff-candidate path — only the coder output is serialized into the completion summary (`_serialize_suggested_memory_entries(coder_output)`), so it is harmless cosmetic cleanup; (2) inbox ranking / near-dup grouping (M5 design open-decision #4) to verify-or-track as a small display-only follow-up. `docs/status/current-state.md` reconciled to this state; see Appendix E.1/E.2 (reconciled 2026-06-14) for the cycle window.
- **Latest update (2026-06-12): Phase 4 is COMPLETE — and with it Area A (Pipeline) Pass 1. The final slice, item 17b (provider prompt caching), is DONE — merged via PR #290 into `develop` (commit `e2b7cc9`, "Add provider prompt cache marker"); its predecessor item 17a (trivial-task stage profile) merged via PR #288 (commits `221cc9c` / `24e24b8`). See the item-17a / item-17b implementation notes below.** Earlier the same day, **item 16 (phase/narrative read-model extending `operator_state`) landed — reviewed before push, then directly on `develop` (commits `c7bfbf4` / `d3deb65`, "Add operator state phase and narrative read model") like item 14; substantive review done pre-push, not via a feature-branch PR.** Additive, display-only: `OperatorState` gains a derived six-phase `phase` (`Planning / Waiting for you / Working / Needs attention / Done / Stopped`) and a structured `narrative` `{what_happened, why, whats_next}`; `schema_version` bumped to 2; no persistence/migration/new-endpoint/new-authority, and every pre-existing field is byte-identical. `whats_next` derives only from computed actions (no phantom/blocked action); unknown/failure/anomaly fail safe to `Needs attention`. The frontend renders the phase chip. **Predecessor: item 15 (reviewer ack soft-gate) DONE — merged via PR #287 into `develop`.** **Items 16, 17a, and 17b have all landed — Phase 4, and Area A Pass 1, is COMPLETE; the rolling implementation brief has no further Phase-4 occupant and is dormant until the next pass (e.g. Area B / Pass 2 memory work) repurposes it.**
- **What:** executing the Pipewright pipeline + memory redesign described in `PIPEWRIGHT_REDESIGN_PROPOSAL.md` (the authoritative, code-verified design).
- **How (the meta-decision):** *not* hand-written PR-by-PR. **Fable 5 owns the full implementation** — it reads the proposal, reasons about edge cases, writes the tests, and implements. A human reviews each completed PR before the next batch starts. Harness for this lives in `FABLE5_IMPL_SPEC_BRIEF.md`.
- **Where we are (2026-06-11):** **Phase 0 is complete and code-verified** (all items present + 105 targeted tests pass; P7 deferred to Phase 2 by design). **§5.1 and §5.2 are now DECIDED** (baseline gate + scoped option default-off; auto-retry budget 1). **Phase 1 is COMPLETE and code-verified** (308 targeted tests pass): item 7 Signal C execution-integrity classification is pure in `backend/pipeline/test_run_validation.py`; item 8 split post-apply test failures into `TEST_REGRESSION` vs. `HARNESS_ERROR` with one non-timeout harness auto-retry (`AUTO_RETRY_INFRA_BUDGET = 1`); item 9 baseline-aware verification (`run_baseline_tests` + `_ensure_verification_baseline` + baseline roll-forward + disclosure in the orchestrator) with the scoped-verification policy knob (`SCOPED_VERIFICATION_ENABLED = False`, default-off, not wired into execution). **Phase 2 is COMPLETE under the per-PR review gate:** item 10 (stage contract) implemented + reviewed & accepted 2026-06-11; item 11 (the driver + T2 rollback move) implemented + reviewed & accepted 2026-06-11; **item 12 (human_retry collapse into the driver + `_execute_retry_attempt` deleted + append-only `chunk_attempts` ledger + P7 resume HEAD-drift) implemented + reviewed & accepted 2026-06-12** — `test_chunked_orchestrator.py` + the item-12 targeted suites green (195 passed). **§5.3 is now DECIDED (2026-06-12, conservative variant).** **Phase 3 item 13 (`steered` attempts on failed chunks) is IMPLEMENTED + MERGED via PR #286 into `develop` (2026-06-12).** **Phase 3 item 14 (post-success refinement: steer a COMPLETED chunk → new commit + cumulative final diff) is IMPLEMENTED 2026-06-12 with tests and AWAITS HUMAN REVIEW (the per-PR gate) — design-first per the brief (`PIPEWRIGHT_ITEM14_DESIGN.md`, human-reviewed before coding); the §0 invariant (a failed refinement never degrades the completed chunk) is pinned by `test_refine_completed_chunk.py`'s headline test; the 563-test parity baseline + 19 new + 87 adjacent tests green, ruff clean on all changed files. PHASE 3 IS COMPLETE under the per-PR gate.** **§5.4 is now DECIDED (2026-06-12) — see the four-§5-decisions section for the full ruling (ack set `high`×{requirement_mismatch,security}, chunk-gate+final, "steer this" bundled into item 15). Next: human review of item 14; then Phase 4 (item 15) is unblocked and can start once item 14 is reviewed.**
- **CRITICAL BOUNDARY:** the parallel batch is endorsed **only for the independent Phase 0 items.** **Stop and reassess at the Phase 1/2 line** — the four §5 decisions *and* the strangler refactor of `_execute_single_chunk`. There, human judgment + a strong-model review gate become load-bearing again. Do not let momentum carry the loop across that line unreviewed.

---

## Why this workflow (and its limits)

The redesign's hard value is in *decisions and edge cases*, not typing. Fable owns both reasoning and implementation. This is sound **for Phase 0** because those items are independent of each other, of every later item, *and* of the §5 decisions — so parallel work doesn't conflict and no model is forced to guess a product decision.

It is **not** sound as a flat fan-out across the whole redesign, for three reasons:
1. **Items past Phase 0 are a dependency chain on shared hot files** (`chunked_orchestrator.py`, `patch_failures.py`, `tester.py`, the policy object). Parallel work would conflict at exactly the integration seams.
2. **Four §5 decisions are the user's, not a model's.** Implementing a dependent item before they're decided routes a product decision to a model — the exact anti-pattern Pipewright exists to prevent.
3. **Phase 2 is a behavior-preserving refactor of the apply/commit/rollback core** — the place a subtle miss becomes a safety regression. It needs a human review gate, not unsupervised implementation.

---

## The documents — which is canonical

| Doc | What it is | Status |
|---|---|---|
| **`PIPEWRIGHT_REDESIGN_PROPOSAL.md`** | The authoritative, code-verified design. Pass 1 = pipeline engine (E-items, §1–7), Pass 2 = memory (M-items, §8+), plus addenda. Contains the §5 decision points and §6 phasing. | **Canonical. Supersedes the earlier reviews.** |
| `ARCHITECTURE_REVIEW.md` | Older, code-*unverified* bug list (P1–P8 pipeline, M1–M7 memory) written for Fable. | Mostly **subsumed** by the proposal (which corrected several of its claims as stale). Keep only for the 3 gaps below. |
| **`FABLE5_IMPL_SPEC_BRIEF.md`** | **The active working doc.** Harness telling Fable how to approach per-task implementation; first task block = E2. | **In use.** |
| **`PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md`** | **The rolling per-phase implementation handoff brief.** Repurposed each phase; held the item-17, Row 11, Row 12, Row 7b, Row 19, Phase 2F, and Phase 2G closeouts. Previously held items 10-16 in turn (each superseded; outcomes live in this workplan). The Phase 2F design brief + closeout lives at `docs/design/phase-2f-thread-ui.md` (§13); the Phase 2G design spec + closeout lives at `docs/design/phase-2g-run-detail-product-ui.md`. | **PHASE 2G RUN DETAIL PRODUCT UI COMPLETE & MERGED (frontend presentation/composition only) - PR-1, PR-2, PR-4, PR-5, PR-3, PR-6.** Phase 2F, Row 19, Row 12, and Row 7b are also complete. Row 16 PR-C activation, Row 23 vector memory, and Phase 2F PR-5 (event persistence) remain deferred. |
| **`PIPEWRIGHT_ITEM7_M5_DESIGN.md`** | The design brief for §23 order-row 7 (M5 suggestion-quality gate); carries a 2026-06-14 closeout header. | **Done — shipped as PR #292.** Historical record + the two tracked non-blocking loose ends. |
| `FABLE5_DESIGN_BRIEF.md`, `FABLE5_ISOLATION_ADDENDUM.md`, `FABLE5_PASS3_BRIEF.md` | The design briefs that *produced* the proposal. | Historical input. |
| `PIPEWRIGHT_REDESIGN_BRIEF.md`, `PIPEWRIGHT_REDESIGN_UI_MOCKUP.svg` | The originating brief + a UI mock. | Input / reference. |

**Current status override (2026-06-12):** item 15 is reviewed + merged (PR #287); **item 16 is DONE — reviewed before push, then landed directly on `develop`** (commits `c7bfbf4` / `d3deb65`) like item 14 (substantive review done pre-push, not via a feature-branch PR). `PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md` held the item-17 design brief; **item 17a (trivial-task stage profile) merged via PR #288 and item 17b (provider prompt caching) via PR #290 — Phase 4, and Area A Pass 1, is now COMPLETE.**

### What the proposal does NOT cover (don't lose these)

From the 2026-06-10 coverage check of `ARCHITECTURE_REVIEW.md` against the proposal, three items have no home in the proposal:
1. ~~**P7** — branch-HEAD drift check on *resume*~~ ✓ **done (2026-06-12, as part of Phase 2 item 12).** `_verify_resume_head_matches_last_attempt` does the exact-SHA check against the attempt ledger's last completed HEAD, degrading to the legacy fail-closed checks when no HEAD was recorded — closing the foreign-commits-stacked-on-top gap without false-positive dead-ends. (Was DEFERRED to Phase 2 on 2026-06-11 precisely because a correct version needs the ledger's recorded per-chunk HEADs.)
2. ~~**ARCH-M1**~~ ✓ **done (2026-06-11)** — the `project_id IS NULL` sweep is now a one-time startup migration (`migrate_unscoped_pre_m1_memory`), removed from `add_fact` / `load_hard_facts` / `list_all_facts`.
3. **P2 (outbox/saga)** — the proposal judged the double-commit risk "already mitigated" by `_verify_completed_checkpoint_safe` and declined the outbox table. Revisit only if you disagree with that call.

---

## Sequence (proposal §6)

- **Phase 0 — independent, decision-free, parallel-safe** *(the endorsed band)*:
  - ~~E2 `stdin=DEVNULL`~~ ✓ **done**
  - ~~E9 scope-intent parser~~ ✓ **done**
  - ~~E8 main-path symmetry (`files_expected` into planner + dry-run before apply)~~ ✓ **done**
  - ~~E4 shared retry executor (delete the 60s lock-held sleeps)~~ ✓ **done**
  - ~~§8b policy-module hygiene (dead constants → new policy module)~~ ✓ **done**
  - ~~test-command auto-detection~~ ✓ **done + wired** *(POST /projects/detect returns `suggested_test_command`; New Project form prefills it without overwriting user input)*
  - ~~P7 branch-HEAD drift on resume *(ARCH-review item)*~~ ✓ **done (2026-06-12, in Phase 2 item 12)** — was deferred to Phase 2 for the attempt ledger's recorded HEADs
  - ~~ARCH-M1 no-op table scan fix~~ ✓ **done** *(ARCH-review item; one-time startup migration)*
- ~~**Phase 1 — needs §5 decisions:** Signal-C infra classifier; split `TEST_FAILURE_AFTER_APPLY` → regression/harness; baseline-aware verification.~~ ✓ **done** (§5.1 + §5.2 decided; all three items implemented + 308 targeted tests pass)
- **Phase 2 — strangler refactor: COMPLETE (2026-06-12).** extract stages → driver replaces `_execute_single_chunk` → retry/resume collapse into entry modes + attempt ledger.
  - **Item 10 (stage contract): IMPLEMENTED 2026-06-11, reviewed & accepted 2026-06-11** (the Phase-2 per-PR gate passed). Golden snapshots captured **pre-refactor** in `backend/tests/test_golden_chunk_execution.py` (6 scenarios, run green before any change); `backend/pipeline/stage_contract.py` adds `OutcomeClass`, the total `PatchFailureType→OutcomeClass` map, `StageOutcome`, and thin adapters; the orchestrator's only change is importing `classify_test_failure`/`build_test_failure_report` from their new canonical home (aliased, behavior-identical). No caller/control-flow change; outcome class is carried, not acted on.
  - **Item 11 (the driver + rollback move): IMPLEMENTED 2026-06-11, reviewed & accepted 2026-06-11 (per-PR gate passed).** New `backend/pipeline/chunk_driver.py` (driver loop + `EntryMode` dispatch: `fresh`/`resume` live, `human_retry`/`steered` are `NotImplementedError` seams, `auto_retry` internal-only). `_execute_single_chunk` is now a thin fresh-mode driver call (signature kept); the resume skip-logic is the driver's `resume` mode (`_verify_completed_checkpoint_safe` discipline verbatim, fail-closed raise preserved); the inline auto-retry block is **deleted** and re-expressed as the driver's single budgeted loop (gate = verify-stage `HARNESS_ERROR` + non-`TIMEOUT` integrity + clean tree — never the outcome class; §11.2a landmine #1 pinned by a new pre-refactor golden: apply-phase `UNKNOWN_PATCH_FAILURE` → zero auto-retries). **T2:** `tester.py` returns verdicts only; the rollback call site moved to `chunk_driver.run_tests_with_rollback` (same trigger/result, runs on the same worker thread, report still built post-rollback). One necessary deviation from the brief's "don't touch `_execute_retry_attempt`": its verify step now routes through the same driver helper, because the tester-side rollback it relied on no longer exists — semantics unchanged, pinned by `test_human_retry_failed_tests_roll_back_via_driver`. All 7 goldens green through the driver; rollback-move equivalence (missing- and double-rollback detectors) + resume-mode + entry-mode-seam tests in new `backend/tests/test_chunk_driver.py`; full unit suite 2792 passed. **Item 12 must not start until a human reviews this PR.**
  - **Item 12 (human_retry collapse + attempt ledger + P7): IMPLEMENTED + reviewed & accepted 2026-06-12 (per-PR gate passed).** `human_retry` is now a driver `EntryMode` (re-runs from the `code` stage with the same approved plan, inside `active_project`); the duplicated `_execute_retry_attempt` is **deleted**, and both `_retry_failed_chunk_locked` and `_approve_and_retry_scope_expansion_locked` re-express their retry as `chunk_driver.drive_chunk(EntryMode.HUMAN_RETRY, …)`. Eligibility unchanged (still derived from the `patch_failures.py` frozensets, never the coarse outcome class). **Attempt ledger:** new append-only `chunk_attempts` table (`schema.sql` + guarded `_ensure_chunk_attempts_shape` in `database.py`, additive/idempotent, metadata-only — no prompts/diffs/secrets), one row per driver pass via `chunk_attempt_store.record_chunk_attempt`; approval commits (which happen outside the driver) record their HEAD via `_record_approval_completion_attempt`. Ledger writes are best-effort/swallowed so audit never breaks the pipeline. **P7:** `_verify_resume_head_matches_last_attempt` adds an exact-SHA resume drift check against the last completed attempt HEAD; degrades to legacy behavior when no HEAD was recorded (pre-ledger runs), so no false-positive dead-ends. **Review finding fixed (test-only):** the new approval-ledger row activated P7 in the existing `test_resume_after_chunk_approval_continues_next_pending_chunk`, which the shared `patch_resume_git` fake didn't cover (it never stubbed the HEAD read); fixed by stubbing `get_current_hash` in that helper (default `"hash"`, drift tests override) — production path unchanged. Added an explicit P7 no-false-positive test (`test_resume_proceeds_when_attempt_head_matches`, covering both the matching-HEAD and pre-ledger-degradation cases). `test_chunked_orchestrator.py` + `test_chunk_driver.py` + `test_golden_chunk_execution.py` + both scope-expansion suites green (195 passed); ruff clean. **Phase 2 is complete; Phase 3 must not start until the §5.3 decision is made and the PR is reviewed.**
- **Phase 3 — needs Phase 2 (done) + §5.3 (DECIDED 2026-06-12, conservative variant):** continuation / steer turns.
  - **Item 13 (`steered` attempts on failed chunks + turn log + budgets in policy): IMPLEMENTED 2026-06-12, awaiting human review (per-PR gate).** `EntryMode.STEERED` filled in `chunk_driver.py` as `human_retry` + continuation context (shared `_HUMAN_ATTEMPT_MODES` path — no fork; steered never auto-retries internally); continuation context (prior coder handoff + prior applied diff *as text* from the patch checkpoint + classified failure evidence + the steer) built in the orchestrator and threaded additively through `code_stage` → `run_coder` (kwarg only passed when set, so steer-less calls are byte-identical). Conservative §5.3 pre-check reuses `file_scope_intent.extract_user_file_constraints` (all mention buckets, fail-safe) against **effective** scope (the `get_chunk_plan_status` overlay), refusing with a re-confirm/#27 offer (`steer_needs_scope_confirmation`, 409, zero mutation); explicit `confirm_in_scope` proceeds with `scope_guard` unchanged as backstop. **Turn log:** append-only `run_turns` table (`schema.sql` + guarded `_ensure_run_turns_shape`) + `run_turn_store.py` (steer text sanitized via `sanitize_for_log`; metadata only); one row per executed steer, linked to its `chunk_attempts` row; refusals mutate nothing; `feature_description` immutable. **Budget:** `policy.HUMAN_ATTEMPT_BUDGET = 2` (relocated `MAX_HUMAN_RETRIES`, value kept; proposal's 5 is a flagged maintainer option) shared across human+steered via the existing `count_human_retry_attempts`; eligibility = new `evaluate_patch_steer_eligibility` over `_RETRY_WITH_INSTRUCTION_TYPES` sharing the retry evaluator's gates. **Deliberate behavior change:** `TEST_REGRESSION` added to `_RETRY_WITH_INSTRUCTION_TYPES` (proposal §4.2 "un-dead-ending TEST_REGRESSION (steerable)" — the item-8 split had left it out), so regression reports now advertise `retry_with_instruction`; plain retry still excluded (golden updated with a named comment). Route: `POST /runs/{run_id}/chunks/{n}/steer` (the brief's "#26D3 unwired" note was stale — the retry route already existed; the steer route is its sibling). Tests: `backend/tests/test_steered_chunk.py` (28) + updated driver-dispatch test + golden; 613 targeted+collateral green, ruff clean; human_retry parity pinned against the pre-change baseline (same 30 retry tests, same goldens).
  - **Item 14 (post-success refinement: steer a COMPLETED chunk → new commit + cumulative final diff): IMPLEMENTED 2026-06-12 with tests, AWAITS HUMAN REVIEW (per-PR gate) before Phase 4.** Design-first (the brief's discipline): `PIPEWRIGHT_ITEM14_DESIGN.md` written + human-reviewed (the user narrowed `REFINABLE_RUN_STATUSES` to exclude `failed`) before any code. **Mechanism:** one steer surface — `chunked_orchestrator.steer_chunk` dispatches on the chunk's fresh in-lock status (`failed` → item-13 path unchanged; `completed` → new `_refine_completed_chunk_locked`; else → 422 `chunk_not_steerable`). A new `chunk_driver.RefinementContext` threaded through `EntryMode.STEERED` inverts exactly two driver behaviors (no loop reshape): failure-finalization becomes restore-to-completed (`_finalize_failed_refinement`, the **§0 invariant** — a failed refinement rolls back to the chunk's commit, leaves `chunk_status=="completed"` + the commit + committed summary intact) and the success pause stores the item-13 `recovered_patch_review` marker carrying a new optional `refinement` block (`_pause_refined_chunk`). **D1:** new commit only on chunk re-approval through the unchanged `_commit_and_complete_chunk`; the no-op refinement is intercepted in `_approve_chunk_and_commit_locked` (clean tree / no files → `refinement_no_op`, never marks the good chunk failed); reject restores `completed` (`_reject_chunk_and_rollback_locked`) instead of failing the run. **D2:** shared per-chunk `HUMAN_ATTEMPT_BUDGET` counted from the `chunk_attempts` ledger via new pure `count_human_attempt_ledger_rows` (a completed chunk has no failure report, so report-based counting can't apply — flagged deviation from the brief's parenthetical, substance preserved). **D3:** cumulative `start_head_sha..HEAD` diff added to `_build_final_approval_summary`, head+tail capped by new `policy.FINAL_DIFF_MAX_CHARS = 20000` (fixes today's no-diff-at-all gap); display-only, never persisted. **Final-gate re-open (#281 generalization):** `approval_gate.supersede_pending_final_gate_and_mark_run` flips the stale pending final gate to a new `GateStatus.SUPERSEDED` atomically (both rowcounts checked → lost race = clean 409) and `_create_final_approval_gate_for_conn` re-creates over a superseded gate; never two pending, never an orphan. **Traps found beyond the brief:** (a) the chunk-gate creator raises on an `approved` latest gate (a human-reviewed completed chunk) → new explicit `allow_after_approved` opt-in used only by the refinement re-pause; (b) `get_latest_completed_attempt_head` ordered by chunk_number, which a low-chunk refinement desyncs from HEAD → re-ordered by `created_at DESC` (would otherwise brick resume); (c) reject-of-refinement (unnamed in the brief) restores `completed` per contract §9. **Files:** `policy.py`, `core/statuses.py`, `git/local_git.py` (`diff_range`/`show_commit`), `patch_failures.py`, `chunk_attempt_store.py`, `approval_gate.py`, `chunk_driver.py`, `chunked_orchestrator.py`, `routes/chunks.py` (route → `steer_chunk`, `failure_report_id` optional), new `backend/tests/test_refine_completed_chunk.py` (19 tests incl. the §0 headline). **Parity:** the 563-test pre-change baseline (174 steer/driver/retry-route/gates/patch_failures + 389 orchestrator/routes/ack/operator) stays green; +19 new + 87 adjacent (golden/reviewer/db-conflict/scope/ack) green; ruff clean on all changed files. 3 route-dispatch tests in `test_steered_chunk.py` updated to patch `steer_chunk` (the deliberate dispatch change); the ~20 orchestrator-level `steer_failed_chunk` tests untouched and green (failed-chunk steering unperturbed). **Phase 3 is COMPLETE under the per-PR review gate. Phase 4 still needs the §5.4 decision before it starts.**
- **Phase 4 — COMPLETE (2026-06-12); completes Area A (Pipeline) Pass 1 [item 15 PR #287, item 16, item 17a PR #288, item 17b PR #290]:** reviewer ack soft-gate (item 15) + "steer this" hook; phase/narrative read-model (item 16); trivial-task profile (item 17a) + prompt caching (item 17b). **Item 15 scope from §5.4:** ack-required set `high` × {`requirement_mismatch`, `security`} (others policy opt-in, default off); ack at chunk gate + final (soft expansion of #28F); diff-hash staleness binding reused; fail-closed/unavailable-reviewer guard preserved; one-click "steer this" via the existing `steer_chunk` dispatcher.

### Phase 4 item 15 implementation note (2026-06-12)

**Item 15 (reviewer informed-approval soft gate + limited "Steer this" hook): IMPLEMENTED with tests, AWAITS HUMAN REVIEW (per-PR gate).** The implementation keeps reviewer memory/advice non-authoritative: only delivered, current-review findings with `severity == "high"` and category in {`requirement_mismatch`, `security`} require a human acknowledgement, and the acknowledgement is metadata-only, bound to the current diff hash. Reviewer-unavailable, failed, stale, missing, or indeterminate review state does not become a denial-of-approval vector. The route-level enforcement is intentionally fail-open on store/selector errors and blocks only on a proven current requirement; the acknowledgement route itself remains strict and returns 409 when no current ackable finding exists. The frontend exposes the acknowledgement panel at chunk and final gates and offers "Steer this" only for current completed/failed chunks through the existing `steer_chunk` dispatcher; no awaiting-approval steer surface was added.

**Files:** `backend/pipeline/review_finding_ack_store.py`, `backend/pipeline/policy.py`, `backend/pipeline/chunk_review_read_model.py`, `backend/pipeline/operator_state.py`, `backend/routes/chunks.py`, `backend/db/schema.sql`, `backend/db/database.py`, `backend/models/chunk.py`, `frontend/src/api/client.ts`, `frontend/src/components/ReviewFindingsAckPanel.tsx`, `frontend/src/components/AdvisoryReviewPanel.tsx`, `frontend/src/components/ChunkPlanPanel.tsx`, `frontend/src/components/FinalApprovalPanel.tsx`, `frontend/src/pages/RunDetailPage.tsx`, plus tests in `backend/tests/test_review_finding_ack_gate.py` and `backend/tests/test_policy.py`. **Verification:** pre-change parity snapshot 326 passed; post-change targeted/parity run 377 passed; `frontend` build passed; ruff clean on changed backend files.

**Outcome:** item 15 reviewed + merged via PR #287 into `develop` (2026-06-12); the per-PR gate cleared. `PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md` has been repurposed into the item-16 design brief.

### Phase 4 item 16 implementation note (2026-06-12)

**Item 16 (phase/narrative read-model extending `operator_state`): DONE — reviewed before push, then landed directly on `develop` 2026-06-12 (commits `c7bfbf4` / `d3deb65`, "Add operator state phase and narrative read model") like item 14; substantive review done pre-push, not via a feature-branch PR.** Design approved (D1 recommended phase mapping; D2 additive narrative; D3 one pure projection function). Purely **display-only and additive** — no persistence, migration, new endpoint, route behavior, eligibility, gate, `scope_guard`, commit/rollback, or LLM phrasing change; every pre-existing `operator_state` field is byte-identical and `schema_version` is bumped 1→2. **Mechanism:** `compute_operator_state` was split into `_select_state` (the unchanged precedence chain) + a thin wrapper that attaches two derived fields via `dataclasses.replace`: (1) `phase: RunPhase` — `project_phase(state, context)` classifies the *already-selected* state by its visible properties (`unknown_state_warning` → `Needs attention`; `is_terminal` → Done on `complete`, else Needs attention; `waiting_on == SYSTEM` → Planning on `RunStatus.RUNNING` else Working; `waiting_on == NOBODY` → Done; then HUMAN: `push_failed`/`decision_type == NONE`/retryable-patch-failure → Needs attention, else `Waiting for you`) — so the phase can never disagree with the displayed state, and every failure/anomaly/unknown fails safe to `Needs attention`; (2) `narrative {what_happened, why, whats_next}` — `build_narrative(state)` reuses `title`/`explanation` verbatim and derives `whats_next` only from the available actions (primary→neutral→secondary), so it never advertises a blocked/phantom action and adds no new prose (no correctness claim, no raw-evidence leak). **`RunPhase.STOPPED` is defined but currently unreached** — there is no explicit cancel/stop terminal status in the codebase (terminal = complete/failed/rejected/final_rejected); the value is wired so a future cancel status lands correctly without re-deriving the logic. The frontend renders a phase chip (`OperatorAttentionPanel`); the narrative's three parts are the already-rendered title/explanation/actions, so no duplicate section was added.

**Files:** `backend/pipeline/operator_state.py`, `frontend/src/api/client.ts`, `frontend/src/components/OperatorAttentionPanel.tsx`, plus tests in `backend/tests/test_operator_state.py` (33 new item-16 tests: phase totality + safety, D1 boundaries, unknown fallback, narrative structure, `whats_next` ⊆ computed actions, no-correctness-claim, no-raw-evidence-leak, existing-fields-unchanged). **Verification:** `backend/tests/test_operator_state.py` 148 passed; route-level surfacing parity (`test_chunk_ack_read_model.py` + `test_reviewer_read_model.py` + `test_review_finding_ack_gate.py`) 63 passed; full unit suite green; `frontend` build passed; ruff clean on `operator_state.py` + the test file.

**Next:** item 16 landed on `develop`; then **item 17a (PR #288)** and **item 17b (PR #290)** landed — see the item-17a / item-17b implementation notes below. **Phase 4 is COMPLETE.**

### Phase 4 item 17a implementation note (2026-06-12)

**Item 17a (trivial-task stage profile): DONE — merged via PR #288 into `develop` (2026-06-12, commits `221cc9c` / `24e24b8`).** Design-first via `PIPEWRIGHT_ITEM17_DESIGN.md` (human-reviewed before code, per the harness). A **cost optimization only, with zero authority change**: for a *provably trivial, eligible, sampled* **fresh** chunk, the driver synthesizes a deterministic `PlannerHandoff` from the already-approved triage instead of calling the planner LLM — and nothing else moves. **Mechanism:** new pure `backend/pipeline/stage_profile.py` (`StageProfile` enum, total `is_trivial_eligible` predicate, stable `sampling_bucket`, `resolve_stage_profile`, `synthesize_trivial_plan`); `_resolve_fresh_stage_profile` + `_synthesize_trivial_plan` in `chunked_orchestrator.py` resolve and thread the profile per chunk; `chunk_driver._drive_stages` branches **only** the fresh `plan_stage` (synthesized `StageOutcome(stage="plan", SUCCESS, evidence=("synthesized_from_triage",))` vs. the planner call) and converges on the identical preflight→apply→verify→**review**→gate→commit loop — nothing below the plan stage is profile-aware. **Eligibility** (single chunk, `complexity=easy`, `risk_level=low`, not `requires_human_review`, no `depends_on`, non-empty grounded `files_expected`) fails toward `STANDARD` on any doubt; a **conservative force-standard denylist** (`policy.TRIVIAL_PROFILE_DENYLIST_PATTERNS`: migrations/`schema.sql`/auth/secrets/dep-manifests/CI) plus an existing-regular-file check make **new-file chunks ineligible**. Behind `policy.MERGED_PROFILE_SAMPLE_PCT` (soak **default 50**, stable per `run_id+chunk_number`); **`0` ⇒ byte-identical to today** (orchestrator returns `None` before reading repo state → standard planner + NULL ledger). **Ledger:** additive nullable `stage_profile TEXT` on `chunk_attempts` (`schema.sql` + idempotent `_ensure_chunk_attempts_shape` + `record_chunk_attempt` param), closed enum `standard | merged_plan_code`, **NULL for legacy rows and every non-fresh pass** (retry/steer/refine/resume) — metadata only, **never read as authority**. **Untouched:** triage, coder, reviewer (runs on every trivial chunk), `scope_guard`, preflight, baseline verification, all gates, commit/rollback, PR rules, model selection, the memory block. **Files:** new `backend/pipeline/stage_profile.py` + `backend/tests/test_stage_profile.py`; `policy.py`, `chunked_orchestrator.py`, `chunk_driver.py`, `chunk_attempt_store.py`, `db/schema.sql`, `db/database.py`; plus a production-neutral `scripts/cleanup_backups.py` injectable-clock test fix (`_now_utc`). **Verification:** full backend unit green (2981 passed, 1 skipped, 4 deselected); pre-merge working-tree review found **no must-fix** (one conscious call: the profile ships **active at the 50% soak default**, reversible to `0` with no code change). **Soak monitoring + rollback SQL:** `docs/design/trivial-task-profile-soak.md` (operator-facing — `stage_profile` cohorts, comparison queries over `chunk_attempts`, `MERGED_PROFILE_SAMPLE_PCT = 0` rollback). **No 17b (prompt caching) changes are in this PR.**

### Phase 4 item 17b implementation note (2026-06-12)

**Item 17b (provider prompt caching): DONE — merged via PR #290 into `develop` (2026-06-12, commit `e2b7cc9`, "Add provider prompt cache marker").** Design-first via `PIPEWRIGHT_ITEM17_DESIGN.md` §7–8 (human-reviewed before code). A **billing optimization only, with zero quality and zero authority change**: byte-identical prompt text to the model; only provider cache **metadata** can differ, and only when explicitly enabled. **Mechanism:** an advisory `cache: bool = False` marker on `Message` (`backend/llm/base.py`); planner/coder mark **only** their static `SYSTEM_PROMPT` / `CODER_SYSTEM_PROMPT` system message `cache=True` (both `_build_llm_request` + `_build_correction_request`), never the memory-bearing user/correction messages. **Anthropic (active):** `_translate_messages` emits `system` as a content-block list **only** when `policy.PROMPT_CACHE_ENABLED` **and** a system message is marked — the marked block carries `cache_control: {type: ephemeral}` and a standalone `"\n\n"` separator block preserves the exact joined text, so the concatenated system text is byte-identical to the legacy string; otherwise it returns the identical string as today. **OpenAI / DeepSeek / Gemini / Fake:** unchanged — they read only `role`/`content` and ignore the marker (pinned by regression tests). **Flags (`policy.py`):** `PROMPT_CACHE_ENABLED = False` and `GEMINI_EXPLICIT_CACHE_ENABLED = False` — **both ship OFF, so on merge 17b is byte-identical to today**; no Gemini `CachedContent` handle is created (disabled seam only). Cache hit/miss is behavior-neutral (`_to_llm_response` ignores `cache_read`/`cache_creation` usage and logs no prompt bytes). **Untouched:** model selection (`role_config.py`), temperature/max-tokens/streaming/token-count semantics, the memory block (never cached), and every execution/gate/scope/reviewer path. **Files:** `backend/llm/base.py`, `backend/llm/providers/anthropic.py`, `backend/pipeline/planner.py`, `backend/pipeline/coder.py`, `backend/pipeline/policy.py`, new `backend/tests/test_prompt_cache.py`. **Verification:** `test_prompt_cache.py` 8 passed; targeted provider/LLM/planner/coder/policy unit suite 160 passed (4 API tests deselected); ruff + `git diff --check` clean; pre-merge working-tree review found **no must-fix**. **To realize caching the maintainer flips `PROMPT_CACHE_ENABLED = True`** (Anthropic-routed roles only; the default provider is Gemini, so it stays dormant until a role is pointed at Anthropic).

**Next:** **Phase 4 is COMPLETE — and with it Area A (Pipeline) Pass 1.** No further Phase-4 slice remains. The rolling `PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md` is dormant until the next pass (e.g. Area B / Pass 2 memory work) repurposes it. Outstanding follow-ups are **operational, not code:** monitor the item-17a soak per `docs/design/trivial-task-profile-soak.md`, and flip `PROMPT_CACHE_ENABLED = True` when ready to activate 17b caching for Anthropic-routed roles.

**Post-Pass-1 red-team audit (2026-06-12):** a local-first-readiness adversarial review landed in `docs/audits/area-a-pass1-red-team-audit.md`. Verdict: the safety core (gates, scope, Git/PR, rollback, resume, reviewer ack, 17b caching) was attacked and held; remaining risks are local-first trust/onboarding/file-index/observability issues, catalogued as **F1–F10** (none implemented). Treat that doc as the local-first hardening queue before wider OSS invitation; recommended first PRs are F1 (Windows EOL preservation in the patch path), F2/F3 (indexer extension reach + exact skip-name matching), and F5 (17a soak cohort eligibility label + soak-doc refresh, which also corrects the stale "17b not implemented" note in `docs/design/trivial-task-profile-soak.md`). The audit explicitly does **not** start Area B or implement any fix.

---

## Phase 0 implementation batching

Fable implements all remaining Phase 0 items. Because some touch shared files, they run in three batches — each batch merges cleanly before the next starts.

| Batch | Items | Key files touched | Can run in parallel? |
|---|---|---|---|
| ~~**A**~~ | ~~E9 scope-intent parser · test-command auto-detection~~ | ~~`file_scope_intent.py`, new detection module~~ | ✓ **done** |
| ~~**B**~~ | ~~E8 main-path symmetry · E4 retry executor~~ | ~~`planner.py`, `coder.py`, `chunked_orchestrator.py`~~ | ✓ **done** |
| ~~**C**~~ | ~~§8b policy-module hygiene~~ ✓ · ~~ARCH-M1~~ ✓ · P7 (deferred) | stage files + policy module + memory_store | §8b + ARCH-M1 done; P7 deferred to Phase 2 |

All of Phase 0 is now complete; P7 (the one item deferred to Phase 2 for the attempt ledger's recorded per-chunk HEADs) landed 2026-06-12 as part of Phase 2 item 12.

---

## The four §5 decisions (the user's call; they gate Phase 1+)

1. **§5.1 Verification semantics:** ✅ **DECIDED (2026-06-11): accept the regression-vs-baseline gate AND add scoped-verification as a policy option, default OFF.** Chunk gate fails only on *newly failing* tests vs. a recorded baseline; pre-existing failures are disclosed, never charged to the chunk; full suite still gates final approval. Gates Phase 1 item 9.
2. **§5.2 Auto-retry of `INFRA_ERROR`:** ✅ **DECIDED (2026-06-11): auto-retry budget = 1, then pause for a human** (conservative end of the proposal's 1–2 range). `INFRA_ERROR` class only — never code/scope failures. Gates Phase 1 item 8.
3. **§5.3 Steer-without-replan** inside unchanged scope: ✅ **DECIDED (2026-06-12): accept steered attempts inside unchanged approved scope, the CONSERVATIVE variant.** A steer re-runs code generation without re-approving the whole plan only while it stays within the approved chunk and approved `files_expected`; if the steer text appears to mention a path outside effective approved scope, do not silently proceed — pause for explicit human re-confirmation or route to the existing scope-expansion approval flow (#27). `scope_guard` stays the hard authority; free text is advisory only. Gates Phase 3 item 13. **Item 14 (post-success refinement) deferred one slice by decision.**
4. **§5.4 Reviewer ack gate:** ✅ **DECIDED (2026-06-12): accept the reviewer informed-approval soft gate.** A delivered **`high` × {`requirement_mismatch`, `security`}** finding requires an explicit human acknowledgement before Approve is enabled; **other categories are a per-project policy opt-in, default OFF**; `warning`/`info` never gate. **Ack attaches at the chunk gate AND final approval** (a deliberate expansion of #28F, which is final-only and does not gate chunk approval today — kept *soft*: ack, never block). Each gating finding gets a **one-click "steer this"** that routes through the existing `steer_chunk` dispatcher (failed-chunk steer or post-success refinement per the chunk's status) — **bundled into item 15**. Settled (not open decisions): reuse the #28F mechanism + diff-hash staleness binding (a retry/steer/refinement re-runs review → ack goes stale → re-ack); fail-closed read-model + an `unavailable` reviewer delivers no findings, so the gate can **never** become a denial-of-approval vector; ack reason optional/audited. Gates Phase 4 (item 15).

Plus §7 open questions (attempt-budget defaults; whether post-success refinement ships in the first continuation slice; confirm test-command detection can ship immediately).

---

## How to resume (next actions)

1. ~~E2 `stdin=DEVNULL` — done.~~
2. ~~Batch A (E9 + test-command detection) — done.~~
3. ~~Batch B (E8 + E4) — done.~~
4. ~~§8b policy-module hygiene — done.~~
5. ~~ARCH-M1 (one-time startup migration) — done.~~
6. ~~Wire `detect_test_command` into the setup route + New Project form — done.~~
7. ~~**P7 deferred to Phase 2** — fold the resume HEAD-drift check into the attempt-ledger work.~~ ✓ **done (2026-06-12, Phase 2 item 12)** — `_verify_resume_head_matches_last_attempt` is the exact-SHA check over the ledger's last completed HEAD, with legacy degradation for pre-ledger runs.
8. ~~**Before Phase 1:** the user rules on the four §5 decisions.~~ **§5.1 + §5.2 decided 2026-06-11** (the two that gate Phase 1); §5.3/§5.4 still pending but gate Phases 3/4. Do **not** let the loop cross into Phase 2 (the strangler refactor) unreviewed (see CRITICAL BOUNDARY).
9. ~~**Phase 1 sequence (separate PRs, in order):** item 7 Signal C classifier → item 8 split `TEST_FAILURE_AFTER_APPLY` → regression/harness + auto-retry budget 1 → item 9 baseline-aware verification + scoped option default-off.~~ ✅ **all done.** item 7 = `classify_execution_integrity` in `test_run_validation.py`; item 8 = `TEST_REGRESSION`/`HARNESS_ERROR` in `patch_failures.py` (uses §5.2; `TEST_FAILURE_AFTER_APPLY` retained for backward compatibility); item 9 = `run_baseline_tests` + `_ensure_verification_baseline` + baseline roll-forward + `SCOPED_VERIFICATION_ENABLED = False` in `policy.py` (uses §5.1). 308 targeted tests pass. (Handoff brief was the rolling `PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md`, now repurposed for Phase 2.)
10. ~~**Next: the Phase 1/2 boundary (CRITICAL BOUNDARY above).**~~ The user authorized **item 10 only** (2026-06-11); it is implemented with tests (golden snapshots captured pre-refactor; full unit suite run). **Implementation handoff brief: `PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md` (Phase 2 = three serialized PRs, items 10 → 11 → 12, with P7 folded into item 12).**
11. ~~**Next: human review of item 10** (per-PR gate).~~ **Item 10 reviewed & accepted 2026-06-11.** ~~Item 11 cleared to start.~~ **Item 11 IMPLEMENTED + reviewed & accepted 2026-06-11** (driver + rollback move; see the Phase 2 sequence above for the full record — goldens unchanged and green, full unit suite 2792 passes).
12. ~~**Next: human review of item 11** (per-PR gate — the highest-risk hunk of Phase 2: the T2 rollback move out of `tester.py`).~~ **Item 11 reviewed & accepted 2026-06-11** (verified: `chunk_driver.run_tests_with_rollback` same-thread/post-rollback report, no double rollback, resume fail-closed preserved, §11.2a apply-phase no-auto-retry golden, and the endorsed `_execute_retry_attempt` verify-step reroute).
13. ~~**Next: human review of item 12** (per-PR gate — `human_retry` collapse + `_execute_retry_attempt` deletion + attempt ledger + P7).~~ **Item 12 reviewed & accepted 2026-06-12** (verified: human_retry parity through the driver with eligibility still derived from the `patch_failures.py` frozensets; additive/idempotent append-only `chunk_attempts`; P7 exact-SHA resume drift with pre-ledger degradation; a review-found P7/approval-ledger interaction fixed test-only in the `patch_resume_git` fake, plus a new explicit no-false-positive test; 195 targeted tests green, ruff clean). **Phase 2 is COMPLETE.**
14. ~~**Next: Phase 3 item 13 (`steered` attempts on failed chunks).**~~ ✅ **IMPLEMENTED 2026-06-12** per the brief (see the Phase 3 sequence above for the full record: driver seam filled as `human_retry` + continuation context, append-only `run_turns` log, `policy.HUMAN_ATTEMPT_BUDGET`, conservative §5.3 via `file_scope_intent` against effective scope, `TEST_REGRESSION` made steerable per proposal §4.2, steer route added; 613 tests green, ruff clean, parity proven against the pre-change baseline).
15. ~~**Next: human review of item 13.**~~ ✅ **Item 13 reviewed, merged via PR #286 into `develop` (2026-06-12).**
16. ~~**Next: Phase 3 item 14 (post-success refinement + cumulative final diff).**~~ ✅ **IMPLEMENTED 2026-06-12** per the brief (see the Phase 3 sequence above for the full record: design-first via `PIPEWRIGHT_ITEM14_DESIGN.md` (human-reviewed; `failed` excluded from `REFINABLE_RUN_STATUSES`), `steer_chunk` dispatcher, `RefinementContext` through `EntryMode.STEERED` with `_finalize_failed_refinement` (§0) + `_pause_refined_chunk` (D1), ledger-based D2 budget, D3 cumulative diff + `FINAL_DIFF_MAX_CHARS`, atomic final-gate supersede with new `GateStatus.SUPERSEDED`, `allow_after_approved` chunk-gate opt-in, `get_latest_completed_attempt_head` recency-order fix, reject-restores-completed; 563-test parity baseline + 19 new + 87 adjacent green, ruff clean). **PHASE 3 IS COMPLETE.**
17. **Next: human review of item 14** (the per-PR gate — review the §0 invariant proof in `test_refine_completed_chunk.py`, the final-gate supersede/re-create seam in `approval_gate.py`, the ledger-based D2 deviation, and the approve/reject refinement branches). ~~**Then Phase 4 — which STILL NEEDS THE §5.4 DECISION.**~~ **§5.4 DECIDED 2026-06-12** (ack set `high`×{requirement_mismatch,security} + policy opt-in for others; chunk-gate+final soft ack; "steer this" bundled into item 15; #28F mechanism + diff-hash staleness reused; unavailable-reviewer never blocks). **Phase 4 (item 15) is unblocked and starts once item 14 is reviewed.** The planning docs are untracked; do not commit code or these docs unless the maintainer asks.

18. ~~**Next: human review of item 15.**~~ ✅ **Item 15 reviewed + merged via PR #287 into `develop` (2026-06-12).**
19. ~~**Next: Phase 4 item 16 (phase/narrative read-model extending `operator_state`).** Design-first.~~ ✅ **Design approved (D1 recommended mapping; D2 additive narrative; D3 one pure projection function) and IMPLEMENTED 2026-06-12** — see the item-16 implementation note above for the full record (additive six-phase `phase` + `{what_happened, why, whats_next}` narrative, `schema_version`→2, display-only, fail-safe to `Needs attention`, `whats_next` ⊆ computed actions; 148 operator_state tests + 63 surfacing-parity green, frontend build + ruff clean).
20. ~~**Next: human review of item 16.**~~ ✅ **Item 16 reviewed before push, then landed directly on `develop` 2026-06-12 (commits `c7bfbf4` / `d3deb65`) like item 14 — substantive review done pre-push, not via a feature-branch PR.** Review covered the `project_phase` precedence-consistency + fail-safe `Needs attention` default, the additive/byte-identical-existing-fields guarantee, `whats_next` ⊆ computed actions, and the reserved-but-unreached `RunPhase.STOPPED`.
21. ~~**Next: the last Phase 4 slice — item 17 (trivial-task stage profile + prompt caching).** Repurpose `PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md` into the item-17 design brief (design-first per the harness).~~ ✅ **Design-first done (`PIPEWRIGHT_ITEM17_DESIGN.md`, human-reviewed) and item 17a (trivial-task stage profile) IMPLEMENTED + merged via PR #288 into `develop` (2026-06-12)** — see the item-17a implementation note above (deterministic eligibility + denylist, synthesize-from-triage, sample 50% stable, additive nullable `stage_profile` ledger column; full backend unit green, pre-merge review found no must-fix).
22. ~~**Next: item 17b (provider prompt caching)** — the final Phase 4 slice: mark the byte-stable system prompts cacheable at the `LLMRequest`/`Message` layer and translate per provider (Anthropic `cache_control` active; OpenAI/DeepSeek passive; Gemini a flagged seam), behind `policy.PROMPT_CACHE_ENABLED` / `GEMINI_EXPLICIT_CACHE_ENABLED`.~~ ✅ **Item 17b IMPLEMENTED + merged via PR #290 into `develop` (2026-06-12, commit `e2b7cc9`)** — advisory `Message.cache` marker; Anthropic `cache_control` translation (byte-identical text, metadata-only) gated by `PROMPT_CACHE_ENABLED`; OpenAI/DeepSeek/Gemini/Fake ignore the marker; both cache flags ship OFF (byte-identical on merge); no Gemini `CachedContent`. `test_prompt_cache.py` (8) + 160 targeted unit green; pre-merge review found no must-fix. See the item-17b implementation note above.
23. **Phase 4 — and Area A (Pipeline) Pass 1 — is COMPLETE.** All Phase-4 slices (15 PR #287, 16, 17a PR #288, 17b PR #290) have landed on `develop`. The rolling `PIPEWRIGHT_REDESIGN_IMPL_BRIEF.md` has no further Phase-4 occupant and is dormant until the next pass (e.g. Area B / Pass 2 memory work) repurposes it. **Outstanding follow-ups are operational, not code:** monitor the item-17a soak per `docs/design/trivial-task-profile-soak.md` and flip `PROMPT_CACHE_ENABLED = True` when ready to activate 17b caching for Anthropic-routed roles. The planning docs are tracked; commit doc/code changes only when the maintainer asks.

24. **Area B (Pass 2 — Memory) began; Row 11 is COMPLETE; Row 12 PR-A, PR-B, and PR-C are COMPLETE.** ~~**Next: §23 order-row 7 — the M5 suggestion-quality gate.**~~ ✅ **IMPLEMENTED + MERGED via PR #292 (2026-06-14)** — deterministic scorer + objective-junk floor + per-run caps + structured coder handoff channel + additive `quality_score` column; pending-only, content gate intact (see the 2026-06-14 TL;DR entry for the full record). **Post-M5 self-use smoke / closeout recorded** in `docs/testing/memory-m5-suggestion-quality-smoke.md` (Appendix E.1 hard-stop satisfied); two non-blocking loose ends tracked there (the planner bare-string channel is unconsumed/harmless; inbox ranking + near-dup grouping to verify-or-track). **§23 order-row 11 — detection rules-as-data — is COMPLETE:** **PR-A** extracted detection rules with ordered candidate parity; **pre-PR-B fixture hardening** merged; **PR-B** added advisory-only repo reality signals for non-DB dimensions with `db_engine`/DB gate unchanged and no memory mutation; **PR-C** refactored the test-command detector into ordered rules with byte-identical output, no PR-C2/new detector coverage, and no classifier/runtime-validation/frontend/schema/memory/gate/scope/Git/PR behavior changes. **§23 order-row 12 PR-A — request-aware selection scaffolding — is COMPLETE:** `request_context` exists but remains dormant; `request_context=None` preserves existing injection behavior; budgets / estimator are single-sourced in policy; adaptive budget scaffolding exists but remains disabled; `security` / `forbidden_paths` are mandatory safety facts that cannot be `budget_dropped`; mandatory overflow uses `MandatoryMemoryBudgetExceeded`; prompt-preview maps overflow to HTTP 422; `not_relevant_to_request` is defined but never emitted. **No PR-A expansion:** no relevance ordering, relevance omission, D5 activation, human-pinning, per-project off-switch, orchestrator plumbing, schema/frontend/gate/scope/Git/PR/memory mutation/vector/FTS/thread UI change. **§23 order-row 12 PR-B — request-aware relevance *ordering* — is COMPLETE:** coder-only `request_context` plumbing (`run_coder` builds it from `plan.goal` / `feature_description` / `files_to_modify`+`files_to_create` / `continuation_context`); a populated context reorders **only** the non-mandatory relevance tier by a deterministic rung-0 signal (path-token overlap, then content-token Jaccard, reusing the `memory_trust` helpers), tie-broken by the existing `(category, scope, priority, created_at)` key; `request_context=None` byte-identical; all-zero overlap preserves the legacy order; the mandatory safety tier is never scored/reordered/dropped; ordering only — no omission, no `not_relevant_to_request` emission, no D5 activation, no pinning, no off-switch, no grace threshold/relevance floor, no adaptive-budget activation, no `MemoryRetriever`/FTS/vector/embedding work, no planner/triage/prompt-preview plumbing, no schema/frontend/gate/scope/Git/PR/memory-mutation change. **§23 order-row 12 PR-C — relevance omission + priority-based pinning + global off-switch — is COMPLETE (dormant-by-default):** D5 confirmed (proposal §24's "D5 confirmed" note); behind one default-off flag `MEMORY_RELEVANCE_OMISSION_ENABLED` (gating BOTH omission and pinning), omission emits `not_relevant_to_request` for zero-signal relevance facts above `MEMORY_SMALL_STORE_GRACE_THRESHOLD` (12) with the all-zero degeneracy guard preserved, and `priority ≤ MEMORY_PIN_PRIORITY_THRESHOLD` (10) facts join the mandatory tier (never scored/omitted/budget-dropped); the flag ships `False` ⇒ byte-identical to PR-B; no schema/frontend/per-project/planner/triage/prompt-preview/adaptive/retriever/FTS/vector/memory-mutation change. **Row 12 is now COMPLETE — PR-A, PR-B, and PR-C are all merged.** **Deferred:** rows 16 (post-run hygiene, D7), 19 (retriever + FTS), 23 (vector, D6); and the §21 thread UI (22b–22e). **Next step: a maintainer / Claude roadmap review before opening any new row or activating the flag** — the only outstanding Row 12 action is operational (flipping `MEMORY_RELEVANCE_OMISSION_ENABLED` on is a soak decision; the prompt-preview 422 wording update is deferred to that flag-activation PR). `docs/status/current-state.md` reconciled 2026-06-14 (Row 12 closeout).

25. **§23 row 7b plan-gate turns + plan-version lineage are COMPLETE & MERGED (2026-06-16), end-to-end but default-off.** PR-A/PR-B/PR-C/PR-D landed the internal engine, backend route, frontend "Revise plan" affordance, and `PIPEWRIGHT_PLAN_TURNS_ENABLED` env wiring. Stabilization Slices A/B/C then landed the read-only plan-version lineage endpoint, `approved_plan_version` approval binding, and frontend lineage display. Backend/API/DB env-var smoke passed with process-local activation; the revised plan still required explicit approval, the existing approve route worked, and no execution was auto-triggered. Later manual smoke after Slice C confirmed flag-off disabled behavior and flag-on revise/history/approval behavior. The lineage/stamp remains provenance only: no auto-execution, no approval blocker, and no final-approval bypass. Browser visual automation had previously failed locally, but no newer uncompleted browser/manual limitation was recorded in this closeout. Default remains off; Row 16 PR-C activation, Row 23, and thread UI remain deferred.

26. **§23 Row 19 MemoryRetriever/FTS is COMPLETE & MERGED (2026-06-16), default-off and explicit-rebuild-only.** PR-A added the guarded inert SQLite FTS scaffold/table plus explicit rebuild/population lifecycle and no runtime reader; PR-B added the `MemoryRetriever` seam and deterministic rung-0 with byte-identical prompt/memory behavior; PR-C added FTS rung-1 retrieval behind `PIPEWRIGHT_MEMORY_FTS_RETRIEVAL_ENABLED`. Flag-off remains byte-identical to deterministic rung-0/current behavior. Flag-on uses FTS only as an advisory ordering signal over the canonical rung-0 candidate set: FTS never adds, drops, caps, or cross-projects candidates, and mandatory/safety facts are never scored, demoted, omitted, or dropped. The FTS index is derived/rebuildable; explicit rebuild/population is required for flag-on to have effect. There is no rebuild-on-write, no lazy rebuild-on-read, no endpoint, no frontend, no `schema.sql` FTS DDL, no approval/execution/final-approval/Git/PR behavior change, and no Row 23 vector memory started. Current deferred memory work is Row 16 PR-C activation and Row 23 vector/embedding memory; thread UI remains deferred.

27. **Row 19 FTS populate/soak follow-up is COMPLETE & MERGED (2026-06-16).** PR-1 added an explicit guarded manual rebuild CLI. PR-2 added a read-only compare/seed soak harness and `docs/design/row-19-fts-soak-results.md`. Seeded soak passed: included set identical, mandatory tier identical, only relevance-tier order changed, no cross-project facts, deterministic output. Real-project soak safely fell back with zero ordering delta. FTS retrieval remains default-off/dormant. No activation trigger, no rebuild-on-write, no lazy rebuild-on-read, no default-on flip, no endpoint/frontend/`schema.sql`/boot-migration populate, and no Row 23 vector/embedding work. The later approval-write-path rebuild trigger remains deferred and should not be started unless future soak shows real value.

28. **Phase 2F Thread UI / Run Timeline is COMPLETE & MERGED (2026-06-17), read-only.** Shipped through PR-0..PR-4 plus review fixes PR-A/PR-B/PR-C; design brief + closeout in `docs/design/phase-2f-thread-ui.md` (§13). **PR-0** added the backend read-only `GET /runs/{run_id}/timeline` derived from existing persisted tables; **PR-1** added the frontend `useRunTimeline` hook + additive `RunTimeline`; **PR-2** added the read-only `RunTimelineDetail` master-detail panel; **PR-3** promoted the timeline to the primary Run Detail layout and made the existing `OperatorAttentionPanel` sticky/prominent; **PR-4** added the Plain English / Developer view toggle. **PR-A** fixed backend timeline correctness/redaction tests; **PR-B** fixed persisted/live dedupe and timeline refresh; **PR-C** fixed redaction polish, sticky height, the `localStorage` guard, and a11y. **Invariants held:** no PR-5 / event persistence started, no schema or event table, no backend writes, no POST lifecycle handler changes, no approval/final-approval/Git/PR behavior change, no memory-retrieval change, no FTS/Row 19 activation, and no Row 23/vector work — the only backend surface added is the single read-only GET. **Deferred:** Phase 2F **PR-5** (fine-grained event persistence) is the only unshipped slice and needs its own one-page brief; Row 16 PR-C activation and Row 23 vector memory also remain deferred.

29. **Phase 2G Run Detail Product UI is COMPLETE & MERGED (2026-06-17), frontend presentation/composition only.** Design spec + closeout: `docs/design/phase-2g-run-detail-product-ui.md`. **PR-1** merged the two-column cockpit/context shell; **PR-2** merged Running/Needs-review context rail trust facts; **PR-4** merged Done-state PR de-duplication and the authoritative PR rail; **PR-5** merged the Failed-state failure rail; **PR-3** merged decision evidence near the approval cockpit; **PR-6** merged visual/register polish and Plain/Developer mode cleanup. Final state: Run Detail is organized around the cockpit, safety overview, context rail, decision evidence, timeline, and collapsed audit/details. **Invariants held:** no backend behavior changes, no mutation handler changes, no approval/final approval/Git/PR behavior changes, no new actions, no event persistence / Phase 2F PR-5 work, no memory retrieval changes, no FTS/Row 19 changes, and no Row 23 work. **Validation recorded:** build/lint/diff checks passed per slice; PR-3 demo-smoke passed all 10 checks; PR-6 SSR smoke passed running/final-approval/done/failed across Plain and Developer modes; protected-path checks confirmed frontend/docs-only scope per slice.

30. **Row 16b ledger metrics / observability queries doc is COMPLETE (2026-06-18), docs-only.** Added `docs/metrics/ledger-metrics-queries.md` as read-only maintainer guidance for SELECT-only SQLite evidence collection over existing ledger metadata before any dormant-feature activation decision. It does not activate Row 16 PR-C, Row 12 omission, prompt caching, plan turns, FTS/Row 19, Row 23, or event persistence; it changes no runtime behavior, schema, policy flags, memory retrieval, prompt builder, approval/final-approval/Git/PR behavior, or code.

*Note: commit these planning docs only when asked.*
